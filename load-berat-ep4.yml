config:
  target: "http://localhost:3000"
  phases:
    - duration: 180       
      arrivalCount: 500

scenarios:
  - flow:
      - get:
          url: "http://localhost:3000/statistik?page=1&limit=20"